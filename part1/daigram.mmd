graph LR
  subgraph Presentation Layer
    PL[Service API\n(HTTP Controllers, DTOs)]
  end

  subgraph Business Logic Layer
    BLF[HBnBFacade <<interface>>]
    UM[User, Place,\nReview, Amenity\nModels & Services]
  end

  subgraph Persistence Layer
    PR[Repositories / DAOs\n(UserRepo, PlaceRepo,\nReviewRepo, AmenityRepo)]
  end

  PL --> BLF : uses
  BLF --> UM : delegates business logic
  UM --> PR : reads & writes data

